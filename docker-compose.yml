version: '3.8'

services:
  apimason:
    build: .
    container_name: apimason
    ports:
      - "1010:1010"
    volumes:
      - ./server/data:/app/server/data
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      - PORT=1010
